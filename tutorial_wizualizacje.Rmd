---
title: "Tutorial - wizualizacje"
output: html_document
runtime: shiny_prerendered
---

```{r setup, include=FALSE}
library(tutor)
```

Zróbmy obrazek
    
```{r addition, exercise=TRUE}
library(ggplot2)
library(dplyr)
ggplot(iris, aes(x = Sepal.Length, y = Petal.Length, color = Species)) +
  geom_point()
```
